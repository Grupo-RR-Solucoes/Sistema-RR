node_modules/
.next/
.env*
!.env.example
runtime/outputs/
# backup_*/ ja cobre backup_etapa7, backup_correcao_A, backup_fix*, etc.
backup_*/
# scratch/ = sandbox de diagnostico/validacao (nao versionar)
scratch/
runtime/*.log
runtime/*.stderr.log
runtime/*.stdout.log
*.tsbuildinfo
.DS_Store
Thumbs.db

# Datasets de auditoria e arquivos de produção (não versionar — são gerados ou
# de propriedade da Promotiva). regras_promotiva/ FICA versionado por ser
# regra de negócio canônica.
/auditorias/
/producao/
/stress_test_workspace_local/*
!/stress_test_workspace_local/gap_analysis.md
/.claude/

# Supabase CLI local cache
supabase/.temp/

# Brand: arquivos de diagnostico/comparacao gerados por scripts (nao
# versionar — sao saidas auxiliares). PNGs/WebPs canonicos da logo
# (logo-rr-cred-*.{png,webp}) ficam versionados.
public/brand/_*.png
