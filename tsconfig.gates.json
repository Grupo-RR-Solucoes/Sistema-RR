{
  "//": [
    "TIPAGEM DOS GATES — projeto SEPARADO, FORA do build.",
    "",
    "POR QUE ESTE ARQUIVO EXISTE. O gate_projecao_gestor ficou MORTO por",
    "TypeError (montarPayloadGestor ganhou um 2o parametro obrigatorio e a",
    "chamada nao) e ninguem notou, porque nada tipa scripts/. Gate que morre",
    "em silencio reporta verde por AUSENCIA de falha, que e pior que vermelho.",
    "",
    "POR QUE NAO DEVOLVER scripts/ AO tsconfig.json. Decisao de 27/06/2026:",
    "scripts/ esta no exclude do tsconfig principal para o next build do Vercel",
    "nao compilar os diag-* de scratch (o build quebrou no PR #13, em",
    "diag-avista-viva.ts:56). A decisao foi resolver pela EXCLUSAO e NAO",
    "remendar os scratch. Isso continua valendo — este arquivo nao a desfaz:",
    "ele e um projeto a parte, so roda por npm run typecheck:gates, e o",
    "next build nunca o le.",
    "",
    "POR QUE include EXPLICITO e NAO um glob. Um glob tipo scripts/**/*.ts",
    "varre o SISTEMA DE ARQUIVOS, nao o git: pegaria os ~72 arquivos untracked",
    "de scratch e reintroduziria exatamente o problema de 27/06. A lista abaixo",
    "e so de arquivo RASTREADO (git ls-files scripts/).",
    "",
    "CRITERIO DA LISTA (aplicado em 01/08/2026 sobre os 45 .ts/.mts",
    "rastreados em scripts/): entram os que ASSEREM — gate*, golden*, test_*.",
    "Ficam de fora diag-*, medida-*, mapa-*, probe-*, rodar*, import*, seed*,",
    "dump* e gen-*, que sao scratch, importadores ou geradores: nao afirmam",
    "nada, entao morrer em silencio neles nao mente sobre o sistema.",
    "diag-avista-viva.ts, que quebrou o PR #13, e um desses e segue de fora.",
    "",
    "AO CRIAR UM GATE NOVO: acrescente o caminho em include. A lista nao se",
    "atualiza sozinha, de proposito — e o preco de nao varrer untracked."
  ],
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "incremental": false,
    "types": ["node"]
  },
  "include": [
    "scripts/gate-avista-vs-fechamento.mts",
    "scripts/gate-import-percentual.mts",
    "scripts/gate-medida-b-conserto.mts",
    "scripts/gate-medida-c-rota.mts",
    "scripts/gate-srcc-ads.mts",
    "scripts/gate_ads_julho_dois_bugs.ts",
    "scripts/gate_ads_seguro_via_render.ts",
    "scripts/gate_projecao_gestor.mts",
    "scripts/gate_regua_bbts_independe_do_client.ts",
    "scripts/gate_remuneracao_lideranca.mts",
    "scripts/gate_teto_avista_rr.ts",
    "scripts/golden_carteira_vs_metadata.ts",
    "scripts/test_equipe_dashboard.ts",
    "scripts/test_equipes_socio_gestor.ts",
    "scripts/test_gestor_meta.ts",
    "scripts/test_inadimplencia_solucionado.ts"
  ],
  "exclude": ["node_modules", "scratch"]
}
